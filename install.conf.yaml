- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/fish: fish/
    ~/.gitconfig: gitconfig

- create:
    - ~/.config

- shell:
    -
      command: ./setup_homebrew.fish
      description: Installing Homebrew and packages
      stdout: true
      stderr: true
